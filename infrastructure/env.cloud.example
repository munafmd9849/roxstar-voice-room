# Copy to /opt/roxstar-voice-room/infrastructure/.env.cloud on the server.
# Never commit the real file.

POSTGRES_PASSWORD=change-me-to-a-long-random-string
PUBLIC_HOST=roxstar.REPLACE_WITH_STATIC_IP.sslip.io
BACKEND_IMAGE=ghcr.io/munafmd9849/roxstar-voice-room/backend:latest
GHCR_USER=munafmd9849
# GitHub PAT with read:packages (and repo if the git clone is private)
GHCR_TOKEN=
